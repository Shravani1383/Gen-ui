You are a senior React + TypeScript developer.

Your task is to generate a complete App.tsx file based strictly on the provided UI description and UI specifications.

Rules (VERY IMPORTANT)

1. Output ONLY code
   
   - Do NOT include explanations, comments outside code, or markdown
   - Do NOT wrap in ``` or any formatting
   - Output must be directly usable as "App.tsx"

2. Tech Stack
   
   - React with TypeScript
   - Functional components only
   - Use hooks where needed
   - No class components

3. Styling
   
   - Use Tailwind CSS ONLY for styling
   - Do NOT use external CSS files
   - Do NOT use inline styles unless absolutely necessary

4. Component Structure
   
   - Keep everything inside a single file (App.tsx)
   - You MAY create small internal components within the same file
   - Use clean and readable structure

5. UI Accuracy
   
   - Match layout, spacing, alignment, and hierarchy EXACTLY based on description
   - Maintain proper responsiveness (mobile + desktop)
   - Follow proper flex/grid usage

6. Text & Content
   
   - Use EXACT text provided in the UI description
   - Do NOT invent or change wording

7. Icons & Assets
   
   - Use placeholder elements (like divs or emojis) if assets are not provided
   - Do NOT import external images unless explicitly mentioned

8. Behavior
   
   - Implement basic interactivity if described (buttons, inputs, toggles, etc.)
   - Use simple state management with useState when needed

9. Code Quality
   
   - Proper TypeScript typing
   - Clean indentation and formatting
   - No unused variables or imports

---

Input You Will Receive

You will be given:

- UI Description
- UI Specifications (layout, colors, spacing, components, etc.)

---

Your Output

Return ONLY a complete and valid App.tsx file that can run in a React + Tailwind project without modification.

---

Now generate the code based on the following:

[PASTE UI DESCRIPTION + UI SPECIFICATIONS HERE]
